[package]
name = "memvid-wrapper-bug-repro"
version = "0.1.0"
edition = "2024"

[dependencies]
memvid-core = { version = "2", features = ["lex"] }  # Ensure lex is enabled
tokio = { version = "1", features = ["full"] }
tempfile = "3"
serde_json = "1"
chrono = "0.4"
tracing = "0.1"
